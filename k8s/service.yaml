apiVersion: v1
kind: Service
metadata:
  name: fastapi-greet
  namespace: uplus-mready-model
  labels:
    app: fastapi-greet
spec:
  type: NodePort
  selector:
    app: fastapi-greet
  ports:
    - name: http
      port: 8000
      targetPort: http
      nodePort: 32700
      protocol: TCP
